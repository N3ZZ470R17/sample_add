<li>
    <%= gravatar_for user, size: 50 %>
    <%= link_to  user.name, user %>
    <% if current_user.admin? && !current_user?(user) %> 
    &nbsp; | <%= link_to "delete", user, data: { turbo_method: :delete, turbo_confirm: "You sure?"}, class: "btn btn-danger" %>
    <% end %>
</li>