<% provide(:title, 'Edit user') %>
<% provide(:button_text, 'Guardar cambios')%>

<h1>Actualización de perfil</h1>

<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <%= render 'form' %>
        <div class="gravatar_edit">
            <%= gravatar_for @user %>
            <a href="https://gravatar.com/emails" target="_blank" rel="noopener">Cambiar</a>
        </div>
    </div>
</div>